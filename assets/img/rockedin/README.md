Place RockedIn portfolio images here.

When screenshots are ready, add their paths to the RockedIn `images` array in
`index.html`. Prefer optimized PNG or JPEG files named descriptively, such as:

- `rockedin_1.png`
- `rockedin_2.png`
- `rockedin_3.png`
